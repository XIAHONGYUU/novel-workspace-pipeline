# Fixtures

This directory contains synthetic, permission-safe demo workspaces.

They exist for two reasons:

- to make the repository easier to understand on first read
- to give `run_workspace_regression.py` a public default target

The content here is intentionally fake and compact. It is designed to exercise workflow logic, not to represent a real novel.
