# 小说工作区流水线-顶层设计

## 一、目标

这份设计文档讨论的不是单个 skill 的提示词优化，而是把小说处理能力升级成一套真正的“工作区流水线”。

目标是让以下五层能力不再只是一次性分析工具，而是可持续、可续跑、可交接的工作区型能力：

- `novel-chapter-distillation`
- `novel-opening-analysis`
- `novel-protagonist-encyclopedia`
- `novel-outline-analysis`
- `novel-highlight-scenes-analysis`

最终希望达到的状态是：

- 可以在已有工作区上继续推进，而不是每次重开
- 可以明确知道当前做到哪一层、还差什么
- 可以把“初始化 -> 分层分析 -> 校验 -> 补缺 -> 交接”串成稳定流程
- 可以让不同脚本共享同一套状态，而不是靠目录猜测
- 可以让各分析层彼此复用上下文，而不是重复铺设同样的信息

## 二、当前五层能力

当前工作区已经不再只有“主角层 + 大纲层”，而是已经形成五层能力栈：

### 1. 章节蒸馏层

负责：

- 逐章压缩全书骨架
- 固定每章核心推进、状态变化、结构功能和章末钩子
- 给后续所有层提供源文校准底稿

对应 skill：

- `novel-chapter-distillation`

### 2. 开篇分析层

负责：

- 判断前三章是否抓人
- 判断主角首亮相是否清晰
- 判断冲突是否启动
- 判断信息释放与章末拉力是否成立

对应 skill：

- `novel-opening-analysis`

### 3. 主角百科层

负责：

- 建立主角知识库主干
- 固定人物关系、身份变化、成长线和关键能力体系
- 为整书层和高光层提供稳定人物上下文

对应 skill：

- `novel-protagonist-encyclopedia`

### 4. 整书大纲层

负责：

- 划分阶段结构
- 判断主线 / 支线 / 冲突升级
- 诊断高潮节奏与收束
- 给出整书结构判断

对应 skill：

- `novel-outline-analysis`

### 5. 剧情高光层

负责：

- 抽取整本书最强的剧情记忆点
- 判断这些高光为何成立
- 判断它们在全书中的分布是否合理
- 补出“读者为什么会追读”的具体吸引力机制

对应 skill：

- `novel-highlight-scenes-analysis`

## 三、当前现状

目前已经具备的能力包括：

- 初始化脚本
- 校验脚本
- checklist
- skill 主流程说明
- 调用模板
- 工作状态写回

现在已经不是“能不能分析”的问题，而是：

- 可以做多层持久化产出
- 可以在已有工作区上扩层
- 可以跑 validator
- 可以写回校验报告和工作状态

但系统还没有完全升级成“统一流水线”。

### 三-B、核心工作模式：不是自动化闭环，是 AI 指挥协作

整个 workflow 从设计之初就不是一个"脚本自动跑完所有分析"的自动化流水线。它是一个**人机协作系统**：

- **脚本负责**：状态管理、框架搭建、规则校验、交接写回。这些都是"可编码的规则性工作"。
- **AI 负责**：阅读理解原文、结构判断、内容生成、质量判断。这些都是"不可编码的理解性工作"。

**init 生成占位符 → validator 报告 keywords_missing → AI 读原文填充 → validator 通过**——这个循环是设计意图而非缺陷。它的本质是一个**质量闸门**：脚本不会替你写内容（设计上也不应该），但它会反复告诉你"还没到位"，直到 AI 的输出达到可校验的标准。

这决定了 workflow 的产出质量上限取决于执行它的 AI 的理解能力和写作能力——不同的 AI 跑同一套 workflow，产生的分析质量会有显著差异。这不是 workflow 的缺陷，而是它的设计前提。

## 四、当前缺口

如果要把这五层能力真正做成小说工作区流水线，还缺以下几类能力：

1. 通用调度能力
2. 通用状态管理能力
3. 差距识别能力
4. 可续跑能力
5. 跨层桥接能力
6. 回归验证能力

这里的“跨层桥接”已经不该只理解成“主角层 -> 大纲层”，而应理解成：

- 章节蒸馏层给 opening / protagonist / outline / highlight 供底稿
- opening 层给 protagonist / outline / highlight 提供入口判断
- protagonist 层给 outline / highlight 提供人物与关系主梁
- outline 层给 highlight 提供阶段位置与结构背景
- highlight 层反向校验 outline 层和 opening 层的吸引力判断

这些能力不是单个提示词能解决的，而是要通过工作区级别的设计来落地。

## 五、顶层设计原则

后续整个流水线建议遵守以下原则：

### 1. 工作区优先

真相源优先放在工作区里，而不是只留在一次回答里。

### 2. 状态显式化

脚本之间共享状态，尽量不要靠“文件名猜测 + 人工记忆”协作。

### 3. 可续跑优先

长篇小说处理天然适合分阶段推进，不应要求每次从头开始。

### 4. 校验与补缺分离

- validator 负责判断“过没过”
- gap report 负责判断“最缺什么、先补什么”

### 5. 桥接优先于重做

当某一层已经存在时，后一层应建立在其上，而不是再重铺一套重复内容。

### 6. Markdown 是交付层，不一定是唯一数据层

Markdown 方便交接，但中长期建议有共享的中间状态或结构化数据层。

### 7. 层与层之间要允许不对称推进

不同项目不一定严格按同一顺序做到完全闭环。

允许：

- 先有主角层，再补 opening 层
- 先有 opening 层，再补 highlight 层
- 先有 outline 层，再回补章节蒸馏层

但必须显式记录“现在哪些层已经达标，哪些层还没做”。

## 六、建议的流水线结构

建议把整套工作区流水线拆成六个通用环节，而不是只围绕某一层写死：

### 第一环：初始化层

负责：

- 建立工作区
- 复制 / 转换原文
- 生成最小文件骨架
- 探测已有分析层成果

### 第二环：状态识别层

负责：

- 读取工作区状态文件
- 判断当前已有哪几层
- 判断哪些文件只是存在但未达标
- 判断当前最值得推进哪一层

### 第三环：跨层桥接层

负责：

- 读取章节蒸馏层产物
- 读取 opening 层产物
- 读取主角层产物
- 读取 outline 层产物
- 为目标层生成可复用上下文

这一层的目标是减少人工重新理解一遍工作区。

### 第四环：目标层执行层

负责：

- 按当前目标层运行对应流程
- 只补当前所缺层，不重写已成立内容
- 支持 fresh / extend-existing / repair-existing / validate-only

### 第五环：校验与差距层

负责：

- 判断当前是否达到结束标准
- 判断哪几层只是存在但未达标
- 输出下一步最该补什么

### 第六环：交接层

负责：

- 写回校验报告
- 写回最新工作状态文件
- 给出当前结论与下一步建议

交接层的目标不是重复分析，而是沉淀“当前做到哪一步”。

## 七、建议新增的关键组件

### 1. 通用总控脚本

建议文件：

- `run_novel_workspace_pipeline.py`

建议职责：

- 读取工作区状态
- 判断是否需要初始化
- 判断当前最该推进哪一层
- 调用对应层的初始化 / 校验 / 补缺流程
- 自动运行 validator
- 输出“已完成 / 仍缺什么”

如果后续你更偏向保留分层总控，也可以并行保留：

- `run_outline_pipeline.py`
- `run_highlight_pipeline.py`

但从顶层设计看，应该先有一个通用入口。

### 2. 通用工作区状态文件

建议文件：

- `workspace-status.json`

建议记录字段：

- `workspace_path`
- `novel_name`
- `protagonist_name`
- `source_files`
- `available_layers`
- `completed_layers`
- `incomplete_layers`
- `last_run_layer`
- `last_validator_result`
- `recommended_next_layer`
- `recommended_next_step`
- `updated_at`

如果要再细一点，可以加：

- `layer_status.chapter_distillation`
- `layer_status.opening`
- `layer_status.protagonist`
- `layer_status.outline`
- `layer_status.highlight`

每层建议至少记录：

- `exists`
- `validated`
- `has_placeholders`
- `last_report`

### 3. 通用差距报告脚本

建议文件：

- `workspace-gap-report.py`

建议输出内容：

- 当前最缺的层
- 哪些文件只是“存在”但没达标
- 哪些文件疑似模板残留
- 下一步最值得补的 1 到 3 层

### 4. 通用跨层桥接脚本

建议文件：

- `build_layer_context.py`

建议职责：

- 读取各层已有产物
- 提炼目标层真正需要的上下文
- 生成“当前层可复用上下文摘要”
- 避免每次都把整个工作区重新读一遍

如果需要保留专用桥接，也可以在这个总桥接之下再拆：

- `protagonist_to_outline_bridge.py`
- `outline_to_highlight_bridge.py`
- `chapter_to_opening_bridge.py`

### 5. 证据锚点层

建议不是一开始就全量做引文系统，而是先做“最小可回溯锚点”：

- 章节范围
- chunk 区间
- 关键事件锚点
- 关联的已有文件

这层能力后续会显著提升复查和修补效率。

## 八、建议的数据与文件关系

### 1. 工作区交付层

继续保留 Markdown 作为最终交付层，例如：

- 章节蒸馏骨架
- 开篇判断与分章拆解
- 主角总卡与词条索引
- 大纲总览与阶段拆分
- Top 10 高光与机制分析
- 校验报告
- 工作状态

### 2. 工作区状态层

引入：

- `workspace-status.json`

用于表达“现在做到哪里”。

### 3. 未来可选的数据层

后续可新增：

- `workspace-data.json`

建议容纳：

- 章节骨架
- 阶段列表
- 主线 / 支线
- 核心冲突点
- 高光锚点
- 关键人物关系
- 证据区间

这样未来模板改版、校验升级、索引生成都会更容易。

## 九、建议支持的运行模式

后续流水线建议明确支持以下模式：

### 1. `fresh`

适用于：

- 新书
- 尚无可复用工作区

### 2. `extend-existing`

适用于：

- 已有若干层工作区
- 需要补新层

### 3. `repair-existing`

适用于：

- 某层文件已存在
- 但存在占位、缺层、未达标内容

### 4. `validate-only`

适用于：

- 只做验收
- 不做内容改写

## 十、建议的最小执行链路

如果先做一个最小可用版工作区流水线，建议最短链路如下：

1. 读取工作区
2. 读取 `workspace-status.json`，如果没有则初始化生成
3. 判断当前已有哪几层
4. 判断当前最值得推进哪一层
5. 如果缺章节骨架层，则优先补 `chapter-distillation`
6. 根据当前状态继续推进：
   - `opening`
   - `protagonist`
   - `outline`
   - `highlight`
7. 运行对应 validator
8. 运行 gap report
9. 写回校验报告与最新工作状态

这条链路跑通后，工作区类 skill 才真正从“会分析”进化成“会推进流程”。

## 十一、建议的实现优先级

如果下一阶段只做最重要的几项，建议顺序如下：

### 第一优先级

- `workspace-status.json`

### 第二优先级

- `run_novel_workspace_pipeline.py`

### 第三优先级

- `workspace-gap-report.py`

### 第四优先级

- `build_layer_context.py`

### 第五优先级

- 证据锚点层

### 第六优先级

- `workspace-data.json`

### 第七优先级

- 分题材 profile

这里把状态文件提到总控脚本之前，是因为没有统一状态，总控脚本最后还是会退化成“靠文件名猜”。

## 十二、回归验证建议

这套流水线一旦继续扩展，必须尽早建立固定回归样本。

建议固定保留 `3 到 5` 本样本小说，优先覆盖：

- 升级流
- 多世界流
- 政治 / 势力流
- 恐怖 / 规则流
- 关系 / 群像较强的书

回归检查目标：

- 是否仍会误判占位文件为完成
- 是否仍能识别单书特性
- 是否会错误覆盖已有层成果
- 是否会在已有工作区上正确续跑
- 是否能正确判断“下一层该做什么”

## 十三、明确不做什么

为了避免工作区流水线行为漂移，建议在设计层就明确排除项：

- 不自动重写已经成立的已有层
- 不把模板文件当完成品
- 不把原文复制当分析完成
- 不在缺关键层时宣布完成
- 不把“文件存在”误当“内容达标”
- 不把不同层的职责混在一起

具体来说：

- 章节蒸馏层不是大纲层
- opening 层不是整书层
- 主角层不是全人物百科层
- 高光层不是普通剧情摘要层

## 十四、一句话总纲

这项工作的本质不是继续补几个脚本，而是把小说处理能力做成一套：

可初始化、可读取状态、可跨层复用、可续跑、可校验、可补缺、可交接的小说工作区流水线。
