# 小说工作区流水线-顶层设计

## 一、目标

这份设计文档讨论的不是单个 skill 的提示词优化，而是把小说处理能力升级成一套真正的“工作区流水线”。

目标是让以下两类 skill 不再只是一次性分析工具，而是可持续、可续跑、可交接的工作区型能力：

- `novel-protagonist-encyclopedia`
- `novel-outline-analysis`

最终希望达到的状态是：

- 可以在已有工作区上继续推进，而不是每次重开
- 可以明确知道当前做到哪一步、还差什么
- 可以把“初始化 -> 分析 -> 校验 -> 补缺 -> 交接”串成稳定流程
- 可以让不同脚本共享同一套状态，而不是靠目录猜测

## 二、当前现状

目前已经具备的能力包括：

- 初始化脚本
- 校验脚本
- checklist
- 主流程说明
- 分题材参考库

以 `novel-outline-analysis` 为例，当前已经能做到：

- 初始化 outline 工作区
- 复用已有主角工作区
- 输出标准大纲文件集
- 运行 validator
- 生成持久化校验报告

这说明系统已经不是“不能用”，而是已经进入“可用但还不够像流水线”的阶段。

## 三、当前缺口

如果要把小说 skill 真正做成工作区流水线，还缺以下几层能力：

1. 调度能力
2. 状态管理能力
3. 差距识别能力
4. 可续跑能力
5. 主角层与大纲层桥接能力
6. 回归验证能力

这些能力不是单个提示词能解决的，而是要通过工作区级别的设计来落地。

## 四、顶层设计原则

后续整个流水线建议遵守以下原则：

### 1. 工作区优先

真相源优先放在工作区里，而不是只留在一次回答里。

### 2. 状态显式化

脚本之间共享状态，尽量不要靠“文件名猜测 + 人工记忆”协作。

### 3. 可续跑优先

长篇小说处理天然适合分阶段推进，不应要求每次从头开始。

### 4. 校验与补缺分离

- validator 负责判断“过没过”
- gap report 负责判断“最缺什么、先补什么”

### 5. 桥接优先于重做

当主角层已经存在时，大纲层应建立在其上，而不是再重铺一套重复内容。

### 6. Markdown 是交付层，不一定是唯一数据层

Markdown 方便交接，但中长期建议有共享的中间状态或结构化数据层。

## 五、建议的流水线结构

建议把整套工作区流水线拆成五层：

### 第一层：初始化层

负责：

- 建立工作区
- 复制 / 转换原文
- 生成最小文件骨架
- 探测已有主角层成果

对应能力：

- `init_workspace.py`
- `init_outline_workspace.py`

### 第二层：桥接层

负责：

- 读取主角总卡
- 读取词条总索引
- 读取核心体系总览
- 自动生成 outline 层起步上下文

这一层的目标是减少人工重新理解一遍工作区。

### 第三层：流程控制层

负责：

- 读取当前状态
- 决定接下来该跑哪一步
- 判断哪些层已完成
- 顺序推进阶段划分、冲突、时间地点、配角关系、单书特性
- 最终自动触发校验

这一层建议新增总控脚本：

- `run_outline_pipeline.py`

### 第四层：校验与差距层

负责：

- 判断当前是否达到结束标准
- 判断哪几层只是存在但未达标
- 输出下一步最该补什么

建议分成两个角色：

- `validate_outline_outputs.py`
- `outline-gap-report.py`

### 第五层：交接层

负责：

- 写回校验报告
- 写回最新工作状态文件
- 给出当前结论与下一步建议

交接层的目标不是重复分析，而是沉淀“当前做到哪一步”。

## 六、建议新增的关键组件

### 1. 总控脚本

建议文件：

- `run_outline_pipeline.py`

建议职责：

- 读取工作区状态
- 判断是否已有主角层
- 判断是否需要初始化
- 判断当前缺的层
- 串联生成各类 outline 文件
- 自动运行 validator
- 输出“已完成 / 仍缺什么”

### 2. 工作区状态文件

建议文件：

- `outline-status.json`

建议记录字段：

- `workspace_path`
- `novel_name`
- `protagonist_name`
- `mode`
- `source_files`
- `reused_files`
- `existing_outputs`
- `placeholder_outputs`
- `last_validator_result`
- `recommended_next_step`
- `updated_at`

这份文件的作用，是让 init、pipeline、validator、gap report 能共享同一套状态。

### 3. 差距报告脚本

建议文件：

- `outline-gap-report.py`

建议输出内容：

- 当前最缺的层
- 哪些文件只是“存在”但没达标
- 哪些文件疑似模板残留
- 下一步最值得补的 1 到 3 层

### 4. 桥接脚本

建议文件：

- `protagonist_to_outline_bridge.py`

建议职责：

- 读取主角总卡
- 读取词条总索引
- 读取核心体系总览
- 生成 outline 层起步上下文
- 为后续流程提供“已知主梁”

### 5. 证据锚点层

建议不是一开始就全量做引文系统，而是先做“最小可回溯锚点”：

- 章节范围
- chunk 区间
- 关键事件锚点
- 关联的已有文件

这层能力后续会显著提升复查和修补效率。

## 七、建议的数据与文件关系

### 1. 工作区交付层

继续保留 Markdown 作为最终交付层，例如：

- 大纲总览
- 阶段拆分
- 冲突点与爆发点
- 时间与地点转折
- 配角与主角关系
- 校验报告
- 工作状态

### 2. 工作区状态层

引入：

- `outline-status.json`

用于表达“现在做到哪里”。

### 3. 未来可选的数据层

后续可新增：

- `outline-data.json`

建议容纳：

- 阶段列表
- 主线 / 支线
- 核心冲突点
- 爆发点
- 时间转折
- 地点转折
- 单书特性
- 配角结构关系

这样未来模板改版、校验升级、索引生成都会更容易。

## 八、建议支持的运行模式

后续流水线建议明确支持以下模式：

### 1. `fresh`

适用于：

- 新书
- 尚无可复用工作区

### 2. `extend-existing`

适用于：

- 已有主角工作区
- 需要补大纲层

### 3. `repair-existing`

适用于：

- outline 文件已存在
- 但存在占位、缺层、未达标内容

### 4. `validate-only`

适用于：

- 只做验收
- 不做内容改写

## 九、建议的最小执行链路

如果先做一个最小可用版工作区流水线，建议最短链路如下：

1. 读取工作区
2. 读取 `outline-status.json`，如果没有则初始化生成
3. 判断是否已有主角层
4. 如果无 outline 层则初始化 outline 文件
5. 依次推进：
   - 阶段拆分
   - 主线支线
   - 冲突爆发
   - 时间地点
   - 配角关系
   - 单书特性
6. 运行 validator
7. 运行 gap report
8. 写回校验报告与最新工作状态

这条链路跑通后，工作区类 skill 才真正从“会分析”进化成“会推进流程”。

## 十、建议的实现优先级

如果下一阶段只做最重要的几项，建议顺序如下：

### 第一优先级

- `run_outline_pipeline.py`

### 第二优先级

- `outline-status.json`

### 第三优先级

- `outline-gap-report.py`

### 第四优先级

- `protagonist_to_outline_bridge.py`

### 第五优先级

- 证据锚点层

### 第六优先级

- `outline-data.json`

### 第七优先级

- 分题材 profile

## 十一、回归验证建议

这套流水线一旦继续扩展，必须尽早建立固定回归样本。

建议固定保留 `3 到 5` 本样本小说，优先覆盖：

- 升级流
- 多世界流
- 政治 / 势力流
- 恐怖 / 规则流
- 关系 / 群像较强的书

回归检查目标：

- 是否仍会误判占位文件为完成
- 是否仍能识别单书特性
- 是否会错误覆盖已有主角层
- 是否会在已有工作区上正确续跑

## 十二、明确不做什么

为了避免工作区流水线行为漂移，建议在设计层就明确排除项：

- 不自动重写已有主角卡
- 不把模板文件当完成品
- 不把原文复制当分析完成
- 不在缺关键层时宣布完成
- 不把“文件存在”误当“内容达标”
- 不把全人物百科工作流和整书大纲工作流混在一起

## 十三、一句话总纲

这项工作的本质不是继续补几个脚本，而是把小说处理能力做成一套：

可初始化、可读取状态、可续跑、可校验、可补缺、可交接的工作区流水线。
