# project

这个目录现在使用 Git 管理，并通过以下文件固定当前任务状态：

- `CURRENT_STATUS.md`
  全仓库当前工作指针
- `WORKFLOW.md`
  开始和结束任务时的固定流程

恢复上下文时，优先看：

1. `git status --short`
2. `CURRENT_STATUS.md`
3. 当前项目目录下的 `项目启动清单`
4. 当前项目目录下的 `词条总索引`
