# 小说主角全词条百科调用话术模板

## 推荐默认模板

下次处理新小说时，优先直接用这一句：

```text
用 novel-protagonist-encyclopedia 处理《小说名》，原文在 /绝对路径/原文.txt，主角是主角名。不要停在初始化或首轮诊断，按主角闭环体系流程直接推进：阶段划分、主角总卡、一级词条、总索引、核心体系总览、全书精华总结、第一批关键二级词条，并明确判断骨架完成还是体系闭环完成。
```

这句话最稳，因为它同时固定了：

- 使用哪个 skill
- 小说名
- 原文路径
- 主角名
- 不要只做初始化
- 目标是闭环
- 必须给出完成度判断

## 最短可用模板

如果你想尽量少打字，可以用：

```text
用 novel-protagonist-encyclopedia 处理《小说名》，原文在 /绝对路径/原文.txt，目标直接做到体系闭环完成。
```

## 已知主角时的标准模板

```text
用 novel-protagonist-encyclopedia 处理《小说名》，原文在 /绝对路径/原文.txt，主角是主角名，目标直接做到体系闭环完成。
```

## 主角有别名时的模板

```text
用 novel-protagonist-encyclopedia 处理《小说名》，原文在 /绝对路径/原文.txt，主角是主角名，别名有别名1、别名2，目标直接做到体系闭环完成。
```

## 只做到骨架完成时的模板

```text
用 novel-protagonist-encyclopedia 处理《小说名》，原文在 /绝对路径/原文.txt，目标先做到骨架完成，不要停在初始化。
```

## 继续已有工作区时的模板

```text
继续用 novel-protagonist-encyclopedia 推进 /home/zuoky/project/小说名 这个工作区，目标直接补到体系闭环完成。
```

## 限制试跑范围时的模板

```text
用 novel-protagonist-encyclopedia 处理《小说名》，原文在 /绝对路径/原文.txt，先只处理前 200 个 chunk 做摸底，但仍按闭环流程组织产物。
```

## 极简模板

如果你愿意让我自己判断主角和推进深度，可以用：

```text
按主角闭环体系处理这本小说：/绝对路径/原文.txt
```

但这种写法不如标准模板稳定。

## 建议你下次至少提供的三项信息

- 小说名
- 原文绝对路径
- 主角名（如果你已经知道）

## 一句话建议

以后默认直接复制“推荐默认模板”即可，稳定性最高，最不容易跑偏。
