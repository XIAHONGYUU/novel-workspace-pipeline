# 小说章节蒸馏骨架分析-调用话术模板

## 一、推荐标准话术

如果小说已经有工作区，最推荐直接使用下面这版：

```text
请使用 `novel-chapter-distillation` skill，在已有小说工作区基础上先对《<小说名>》原文做章节蒸馏。

工作区路径：`<工作区绝对路径>`
原文路径：`<原文绝对路径>`

要求：
1. 这一步先于主角层、开篇层、整书层和高光层执行。
2. 每一个章节都要做浓缩精华，不允许只列章节标题。
3. 至少覆盖这些文件：
   - `chapter-distillation-manifest.json`
   - `<小说名>-章节蒸馏骨架.md`
   - `<小说名>-阶段骨架与换挡草图.md`
   - `<小说名>-校准与验证锚点.md`
4. 每一章至少判断：
   - 本章核心推进
   - 主角 / 核心视角状态
   - 关键新信息 / 新设定
   - 关系 / 局势变化
   - 本章结构功能
   - 章末钩子 / 遗留问题
5. 最后形成一份可以给后续所有 skill 用来检查和校准的骨架层。
6. 完成后运行 validator。
7. 把校验报告和最新 `工作状态-YYYY-MM-DD.md` 写回工作区。
8. 最后明确告诉我是否达到：
   - `章节骨架已形成 / 章节骨架仍不足`
   - `校准锚点已可用 / 校准锚点仍不足`
```

## 二、极简调用版

如果你不想写太长，可以直接用这版：

```text
请使用 `novel-chapter-distillation` skill，在已有小说工作区基础上先把《<小说名>》按章节蒸馏成骨架，补齐章节蒸馏文件，跑完 validator，并把校验报告和最新工作状态写回工作区。
```

## 三、新书初始化版

如果是第一次做，还没有现成工作区，用这版：

```text
请使用 `novel-chapter-distillation` skill 为《<小说名>》建立一套章节蒸馏骨架工作区。

原文路径：`<原文绝对路径>`
目标工作区：`<工作区绝对路径>`

要求：
1. 先初始化工作区。
2. 先识别章节，再把每一章压成精华骨架。
3. 再补阶段换挡草图和校准锚点。
4. 完成后运行 validator。
5. 把校验报告和最新工作状态写回工作区。
```

## 四、只检查和补缺口版

如果工作区已经有 chapter distillation 文件，但你想检查质量和补缺，可以用这版：

```text
请使用 `novel-chapter-distillation` skill 检查《<小说名>》当前的章节蒸馏骨架工作区。

工作区路径：`<工作区绝对路径>`

要求：
- 先读取现有章节蒸馏文件
- 运行 validator
- 只补缺口，不重写已经成立的内容
- 最后明确告诉我：
  - 现在哪些层已经达标
  - 哪些层仍不足
  - 是否已经达到 `章节骨架已形成`
  - 是否已经达到 `校准锚点已可用`
```

## 五、强调“这是前置骨架层”的加强版

如果你想明确要求它不要和后续分析混层，用这版最合适：

```text
请使用 `novel-chapter-distillation` skill 先处理《<小说名>》。

这一步是前置骨架层，不要提前做主角百科判断、整书大纲判断或高光桥段判断。

你要做的是：
- 识别章节
- 逐章蒸馏结构精华
- 标出阶段换挡
- 标出后续校准锚点

目标是给后续所有小说 skill 留下一层统一的源文骨架。
```

## 六、一句话口语版

如果你只是平时自己快速调用，可以直接说：

```text
请用 `novel-chapter-distillation` 先把《<小说名>》每章压成精华骨架，做成后续校准用的底稿，最后跑校验。
```

## 七、建议使用顺序

如果是完整小说工作流，建议顺序如下：

1. 先用 `novel-chapter-distillation`
   固定章节骨架、阶段换挡和校准锚点
2. 再用 `novel-opening-analysis`
   看前三章 promise 是否立住
3. 再用 `novel-protagonist-encyclopedia`
   做主角知识库主干
4. 再用 `novel-outline-analysis`
   做整书结构层
5. 最后用 `novel-highlight-scenes-analysis`
   抽整书最强高光桥段

## 八、调用时最值得补充的信息

如果你想让结果更稳，调用时最好尽量补充：

- 小说名
- 工作区路径
- 原文路径
- 当前是否已有其他分析层
- 你更看重“先做全书骨架”还是“先建立校准锚点”

## 九、最短推荐版

如果只保留一条，建议保留这版：

```text
请使用 `novel-chapter-distillation` skill，在已有小说工作区基础上先把《<小说名>》按章节蒸馏成骨架，补齐章节蒸馏文件，跑完 validator，并把校验报告和最新工作状态写回工作区。
```
