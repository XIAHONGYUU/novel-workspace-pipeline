# 小说高光剧情细节分析-调用话术模板

## 一、推荐标准话术

如果小说已经有工作区，最推荐直接使用下面这版：

```text
请使用 `novel-highlight-scenes-analysis` skill，在已有小说工作区基础上分析《<小说名>》里最吸引人的十个剧情细节。

工作区路径：`<工作区绝对路径>`
原文路径：`<原文绝对路径>`
主角名：`<主角名>`

要求：
1. 先读取现有工作区，不要重做主角百科层、整书大纲层，也不要只盯前三章。
2. 默认复用已有上下文，例如主角总卡、词条总索引、核心体系总览、大纲总览、黄金前三章总判断。
3. 重点找出全书最抓人的 10 个剧情细节，不要只写“我觉得哪里好看”。
4. 至少覆盖这些文件：
   - `<小说名>-最吸引人的十个剧情细节总表.md`
   - `<小说名>-剧情吸引力机制分析.md`
   - `<小说名>-Top10细节逐条拆解.md`
   - `<小说名>-高光桥段分布与节奏判断.md`
   - `<小说名>-最强爽点痛点悬念点总结.md`
   - `<小说名>-剧情高光改造建议.md`
5. 每个高光细节至少判断：
   - 事件本身是什么
   - 它发生在什么阶段
   - 前面铺了什么
   - 为什么会在这里爆
   - 它主要打中读者的哪种快感
   - 它改变了什么人物关系或局势
6. 额外判断这些高光桥段在整本书里的分布是否合理。
7. 完成后运行 validator。
8. 把校验报告和最新 `工作状态-YYYY-MM-DD.md` 写回工作区。
9. 最后明确告诉我是否达到：
   - `高光桥段已明确 / 高光桥段仍不足`
   - `剧情吸引力机制已拆清 / 剧情吸引力机制仍不足`
```

## 二、极简调用版

如果你不想写太长，可以直接用这版：

```text
请使用 `novel-highlight-scenes-analysis` skill，在已有小说工作区基础上处理《<小说名>》里最吸引人的十个剧情细节，复用已有上下文，补齐高光剧情文件，跑完 validator，并把校验报告和最新工作状态写回工作区。
```

## 三、新书初始化版

如果是第一次做，还没有现成工作区，用这版：

```text
请使用 `novel-highlight-scenes-analysis` skill 为《<小说名>》建立一套“最吸引人的十个剧情细节”分析工作区。

原文路径：`<原文绝对路径>`
目标工作区：`<工作区绝对路径>`
主角名：`<主角名，可留空>`

要求：
1. 先初始化工作区。
2. 再筛选全书 Top 10 高光桥段，不要只写剧情摘要。
3. 要解释每个桥段为什么成立，而不是只说它很燃、很虐、很炸。
4. 完成后运行 validator。
5. 把校验报告和最新工作状态写回工作区。
```

## 四、只检查和补缺口版

如果工作区已经有 highlight 文件，但你想检查质量和补缺，可以用这版：

```text
请使用 `novel-highlight-scenes-analysis` skill 检查《<小说名>》当前的剧情高光分析工作区。

工作区路径：`<工作区绝对路径>`

要求：
- 先读取现有 highlight 文件
- 运行 validator
- 只补缺口，不重写已经成立的内容
- 最后明确告诉我：
  - 现在哪些层已经达标
  - 哪些层仍不足
  - 是否已经达到 `高光桥段已明确`
  - 是否已经达到 `剧情吸引力机制已拆清`
```

## 五、强调“不要只写爽点列表”的加强版

如果你想明确要求它不要偷懒，用这版最合适：

```text
请使用 `novel-highlight-scenes-analysis` skill 分析《<小说名>》里最吸引人的十个剧情细节。

要求这次分析默认按“机制拆解模式”执行，不允许只给我一个爽点列表。

请重点判断：
- 这 10 个高光桥段分别靠什么成立
- 这本书最主要的吸引力机制是什么
- 高光桥段是集中在前段、中段还是后段
- 有没有中段掉能量或后段纯堆规模的问题
- 哪些高光值得保护，哪些区段值得补强

并且要给出：
- Top 10 总表
- 机制分析
- 逐条拆解
- 分布与节奏判断
- 爽点 / 痛点 / 悬念点总结
- 改造建议
```

## 六、一句话口语版

如果你只是平时自己快速调用，可以直接说：

```text
请用 `novel-highlight-scenes-analysis` 把《<小说名>》全书最抓人的 10 个剧情细节拆出来，重点看它们为什么成立、怎么分布，最后跑校验。
```

## 七、建议使用顺序

如果是完整小说工作流，建议顺序如下：

1. 先用 `novel-opening-analysis`
   看前三章是否能抓人、卖点是否立住
2. 再用 `novel-protagonist-encyclopedia`
   做主角知识库主干
3. 再用 `novel-outline-analysis`
   做整书大纲层与结构层分析
4. 最后用 `novel-highlight-scenes-analysis`
   反向抽取整本书最强吸引力到底落在哪些具体剧情瞬间上

## 八、调用时最值得补充的信息

如果你想让结果更稳，调用时最好尽量补充：

- 小说名
- 工作区路径
- 原文路径
- 主角名
- 是否已有主角层 / 大纲层 / opening 层
- 你更关心“找出最强高光”还是“怎么增强全书吸引力”

## 九、最短推荐版

如果只保留一条，建议保留这版：

```text
请使用 `novel-highlight-scenes-analysis` skill，在已有小说工作区基础上分析《<小说名>》里最吸引人的十个剧情细节，复用已有上下文，补齐高光剧情文件，跑完 validator，并把校验报告和最新工作状态写回工作区。
```
