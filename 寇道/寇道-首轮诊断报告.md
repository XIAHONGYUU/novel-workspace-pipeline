# 《寇道》首轮诊断报告

## 一、结论

这轮自动执行已经完成了工作区初始化、`txt -> md` 和首轮 heuristic pipeline，但首轮人物抽取结果的可用性需要单独判断。

当前判断：`一般`

下一步建议：heuristic 噪声较高，但主角候选最像 `寇立`，建议立即对其跑 focus

## 二、主角候选 Top 10

- `寇立`：文本粗提及约 `9601` 次；别称：无
- `就像是`：文本粗提及约 `594` 次；别称：就像
- `黄公子`：文本粗提及约 `521` 次；别称：黄公
- `罗严宗`：文本粗提及约 `361` 次；别称：罗严
- `五灵子`：文本粗提及约 `318` 次；别称：五灵
- `化作`：文本粗提及约 `262` 次；别称：无
- `古小钗`：文本粗提及约 `229` 次；别称：古小
- `豸剑子`：文本粗提及约 `226` 次；别称：豸剑
- `林素娥`：文本粗提及约 `218` 次；别称：林素
- `好似`：文本粗提及约 `203` 次；别称：无

## 三、heuristic 质量检查

- 合并后候选总数：`468`
- 过滤后较像人名的候选数：`432`
- 前 100 个索引项中的疑似噪声比例：`22%`
- 是否检测到前言 / 序章污染：`是`

## 四、前几个抽取块的直接样本

- `chunk-0001.json` / `前言` -> 寇立, 郑老, 郑老铁, 马脸, 江湖
- `chunk-0002.json` / `前言` -> 寇立, 乌头, 燕飞娘, 而且, 燕飞
- `chunk-0003.json` / `前言` -> 寇立, 郑宝儿, 罗严宗, 郑宝, 便是

## 五、前 12 个 chunk 标题

- 前言
- 前言
- 前言
- 前言
- 前言
- 前言
- 前言
- 前言
- 前言
- 前言
- 前言
- 前言

## 六、当前 checklist 状态

- [x] 整书 chunk 蒸馏已完成
- [ ] 整书阶段划分已完成
- [ ] 主角锚点已稳定
- [ ] 主角总卡已完成
- [ ] 一级词条已齐
- [ ] 总索引已完成
- [ ] 核心体系总览已完成
- [ ] 全书精华总结已完成
- [ ] 最关键的一批二级词条已完成

## 七、解释

这轮首要问题通常出在两处：

- `heuristic` 会把副词、泛称、前言噪声一起当成人名候选
- 首轮自动结果虽然能摸到主角候选，但还不能直接代替主角骨架判断

所以首轮真正的价值应该是：

- 判断主角最可能是谁
- 判断结果噪声是否可接受
- 决定是否立刻进入 `focus` 阶段
